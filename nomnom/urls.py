from django.conf.urls import patterns, include, url

urlpatterns = patterns('',
    # TODO: brilliant URL pattern to switch out the template used for the admin
	# change list so that we can overwrite the object tools ONLY
)
